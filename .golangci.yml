# yaml-language-server: $schema=https://golangci-lint.run/jsonschema/golangci.jsonschema.json
version: "2"
run:
  build-tags:
    - e2e_test
  modules-download-mode: readonly
linters:
  default: none
  enable:
    - asciicheck
    - bodyclose
    - dogsled
    - errcheck
    - exhaustive
    - gochecknoinits
    - gocognit
    - gocritic
    - gocyclo
    - goheader
    - goprintffuncname
    - gosec
    - govet
    - importas
    - ineffassign
    - lll
    - makezero
    - misspell
    - nakedret
    - nilerr
    - prealloc
    - predeclared
    - revive
    - rowserrcheck
    - sqlclosecheck
    - staticcheck
    - tparallel
    - unconvert
    - unparam
    - unused
    - wastedassign
    - whitespace
  settings:
    exhaustive:
      default-signifies-exhaustive: true
    gocognit:
      min-complexity: 30
    gosec:
      excludes:
        - G101 # Look for hardcoded credentials
        - G115 # Potential integer overflow when converting between integer types
        - G117 # Potential exposure of secrets via JSON/YAML/XML/TOML marshaling
        - G204 # Audit use of command execution
        - G304 # File path provided as taint input
        - G703 # Path traversal via taint analysis
        - G704 # SSRF (Server-Side Request Forgery) via taint analysis
        - G705 # XSS (Cross-Site Scripting) via taint analysis
    gocritic:
      disabled-checks:
        - singleCaseSwitch
      enabled-tags:
        - opinionated
    lll:
      line-length: 120
    misspell:
      locale: US
    revive:
      rules:
        - name: unused-parameter
          disabled: true
  exclusions:
    generated: strict
    rules:
      - linters:
          - revive
        text: exported (function|method|type|const|var) .*? should have comment
      - linters:
          - revive
        text: "^var-naming: .*"
      - linters:
          - revive
        text: "error-strings: error strings should not be capitalized or end with punctuation or a newline"
      - linters:
          - staticcheck
        text: (WithDryRun is deprecated)|(Objects\(\).V1\(\).\w+ is deprecated)|(aws-sdk-go is deprecated.)
issues:
  # Value 0 means show all.
  max-issues-per-linter: 0
  max-same-issues: 0
formatters:
  enable:
    - gofumpt
    - goimports
  settings:
    goimports:
      local-prefixes:
        - github.com/nobl9/nobl9-go
    gofumpt:
      extra-rules: true
  exclusions:
    generated: strict
